$("#title").typed({
    strings: ["Seu..!", "Próprio..!", "Site..!" , "Seu Próprio Site"],
    typeSpeed: 300,
    backDelay: 900,
    loop: true,
    cursorChar: "|",
    contentType: 'html', // or text
    // defaults to false for infinite loop
    loopCount: false
});